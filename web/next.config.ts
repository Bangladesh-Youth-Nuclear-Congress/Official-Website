import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* Netlify's @netlify/plugin-nextjs handles the App Router build,
     SSR/ISR and next/image optimization — no extra config needed. */
};

export default nextConfig;
