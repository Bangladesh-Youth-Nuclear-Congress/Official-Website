# Netlify build config — Next.js app lives in web/, deployed as a static export.
[build]
  base = "web"
  command = "npm run build"
  # `next build` with output:"export" writes static files to web/out.
  # publish is resolved relative to base, so this points at web/out.
  publish = "out"
