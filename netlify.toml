# Netlify build config — Next.js app lives in web/
[build]
  base = "web"
  command = "npm run build"

# Netlify auto-detects Next.js and applies @netlify/plugin-nextjs
# (App Router SSR/ISR + image optimization). No publish dir needed.
