# ── Not part of the deployed website ──────────────────────────
# Large / sensitive: sponsorship proposal contains personal contacts (83 MB)
*.pdf
# ...except the public sponsor-facing proposal served on the site (drop the slim PDF here)
!assets/BYNC-Sponsorship-Proposal.pdf

# Internal working & research docs
BYNC_Research_Brief.md
bync-website-prototype.html

# Original (unprocessed) logo sources — processed copies live in assets/
BYNC.png
BYNC JPG.jpg

# Spreadsheets / data
*.xlsx
*.csv

# ── OS / editor cruft ─────────────────────────────────────────
Thumbs.db
.DS_Store
*.tmp
~$*
*.log

# Local Netlify CLI link state
.netlify/

# Local agent/editor config — not part of the deployed website
.claude/
